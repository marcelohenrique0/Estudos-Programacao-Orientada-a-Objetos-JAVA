package Aulas.CriacaoClass.iguais;

public class Iguais {
    public static <T> boolean iguais(T valor1, T valor2){ // metodo generico para comparar dois objetos do mesmo tipo
        return valor1.equals(valor2);

    }

}
